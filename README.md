The goal of the app is to weight and blood pressure and mood on a given day then export that data as a csv to be sent to a health professional. You should also integrate with an LLM for observations.

Foundational choices are:

* Rust for the app layer because - why not? I've learnt Rust and this gives me an excuse to play with it. 
* React for the frontend, ibid.

 