# Sub-task: Rust Core (DB, schema, config)

**Type:** sub-task
**Parent:** 2026-06-05T08-30-50-programme-build-healthy-app.md
**Created:** 2026-06-05 08:30:50
**Model:** qwen3-coder-30b
**Status:** planned

## ⚠️ Do not run any git commands.

## Goal

Update project config files and implement the database layer: migrations, schema structs, and connection init.

## Changes

### src-tauri/Cargo.toml

Replace entirely. Key changes from the scaffold:
- `name = "healthy"`, `[lib] name = "healthy_lib"`
- Remove `tauri-plugin-opener`
- Add: `rusqlite = { version = "0.32", features = ["bundled"] }`, `reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }`, `tokio = { version = "1", features = ["full"] }`, `chrono = "0.4"`

### src-tauri/tauri.conf.json

Update fields only (do not restructure):
- `productName` → `"healthy"`
- `identifier` → `"com.nicholasf.healthy"`
- `beforeDevCommand` → `"pnpm dev"`
- `beforeBuildCommand` → `"pnpm build"`
- `windows[0].title` → `"healthy"`

### src-tauri/src/main.rs

Change `healthy_scaffold_lib::run()` to `healthy_lib::run()`.

### src-tauri/src/db/migrations.rs

Two `pub const` SQL strings:
- `CREATE_ENTRIES`: `CREATE TABLE IF NOT EXISTS entries` with columns `id INTEGER PRIMARY KEY AUTOINCREMENT`, `date TEXT NOT NULL UNIQUE` (YYYY-MM-DD), `weight_kg REAL`, `bp_systolic INTEGER`, `bp_diastolic INTEGER`, `energy_level INTEGER` (0–5), `notes TEXT`, `created_at TEXT NOT NULL DEFAULT (datetime('now'))`
- `CREATE_SETTINGS`: `CREATE TABLE IF NOT EXISTS settings` with columns `key TEXT PRIMARY KEY`, `value TEXT NOT NULL`

### src-tauri/src/db/schema.rs

Two structs deriving `serde::Serialize`, `serde::Deserialize`, `Debug`, `Clone`:
- `pub struct Entry` with `#[serde(rename_all = "camelCase")]`: fields `id: i64`, `date: String`, `weight_kg: Option<f64>`, `bp_systolic: Option<i64>`, `bp_diastolic: Option<i64>`, `energy_level: Option<i64>`, `notes: Option<String>`, `created_at: String`
- `pub struct Setting`: `key: String`, `value: String`

### src-tauri/src/db/mod.rs

- `pub mod migrations; pub mod schema;`
- `pub use schema::{Entry, Setting};`
- `pub struct AppState { pub db: std::sync::Mutex<rusqlite::Connection> }`
- `pub fn init(path: &std::path::Path) -> rusqlite::Result<rusqlite::Connection>`: opens `path/healthy.db`, runs both migration SQL via `conn.execute_batch()`, returns connection

## Done when

- [ ] `src-tauri/Cargo.toml` contains `rusqlite`, `reqwest`, `tokio`, `chrono` and package name is `"healthy"`
- [ ] `src-tauri/tauri.conf.json` productName is `"healthy"` and beforeDevCommand is `"pnpm dev"`
- [ ] `src-tauri/src/db/migrations.rs` defines `CREATE_ENTRIES` and `CREATE_SETTINGS`
- [ ] `src-tauri/src/db/schema.rs` defines `Entry` and `Setting`
- [ ] `src-tauri/src/db/mod.rs` defines `AppState` and `init()`

## Results
<!-- Filled in by the executing model after completion -->
**Tests:**
**Files changed:**
**Summary:**
